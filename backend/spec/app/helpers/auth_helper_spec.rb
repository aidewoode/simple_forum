require 'spec_helper'

RSpec.describe 'Backend::App::AuthHelper' do
  pending 'add some examples to (or delete) #{__FILE__}' do
    let(:helpers) { Class.new }
    before { helpers.extend Backend::App::AuthHelper }
    subject { helpers }
  end
end
